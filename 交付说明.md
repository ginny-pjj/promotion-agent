# 推广申请处理 Agent — 交付说明

## 一、交付内容清单

| 交付项 | 位置 / 说明 |
|---|---|
| 可运行演示 | `promotion-agent/`，见下方启动方式 |
| 代码 | 本目录全部源码 |
| 简短说明 | 本文档 |
| 测试表模板 | `data/promotion_accounts_template.xlsx` |
| 负责人配置 | `config/responsible.json` |

---

## 二、如何演示（面试前先跑通）

```powershell
cd promotion-agent
pip install -r requirements.txt
python -m uvicorn web_app:app --reload
```

浏览器打开：`http://127.0.0.1:8000`

1. 粘贴抖加申请 + 垫付申请（或点「加载示例」）
2. 点「开始处理」
3. 查看金额汇总、异常清单
4. 若有异常 → 出现「已转人工确认」+ 人工确认单
5. 下载「推广账号表测试副本」用 Excel 打开核对

命令行演示：

```powershell
python main.py --demo --template data/promotion_accounts_template.xlsx --output output_test
```

---

## 三、实现思路

### 业务目标

每天收到两类非结构化文本（抖加申请、垫付申请），需要：

1. 抽出账号、金额、支付人、打款日期
2. 核对总额 / 笔数 / 日期是否合理
3. 写入推广账号表副本（不改原表）
4. **有异常时转人工负责人，系统不自动放行**

### 架构分层

```text
输入文本
   ↓
抽取层（extractor.py）     ← 当前用正则；后续可换大模型
   ↓
规则层（validator.py）     ← 金额、笔数、日期、重复、未匹配账号
   ↓
人工确认层（human_review.py）← 有异常 → 生成人工确认单，指派负责人
   ↓
输出层（excel_writer + report）← 测试表副本 + 汇报
```

### 核心设计原则

- **大模型只做「理解」**（字段抽取、文案），不做「裁决」
- **金额和异常判定永远用 Python 规则**，保证可追溯、可审计
- **原表只读，只写测试副本**，避免误改正式数据
- **有问题必须人工确认**，AI/规则不能代替负责人做通过决定

### 面试可讲的核心代码

1. `validator.py`：声明总额 vs 明细合计、笔数、日期异常、重复申请
2. `excel_writer.py`：按「抖音昵称」匹配写入抖加/打款字段，异常行标「待人工确认」
3. `human_review.py`：有 `issues` → `needs_review`，生成《人工确认单》指派负责人

---

## 四、遇到的问题

| 问题 | 处理 |
|---|---|
| Excel 在 VS Code 里打开是乱码 | `.xlsx` 是二进制，需用 Excel/WPS 打开；程序用 openpyxl 读写 |
| 模板文件名变成 `.xlsx.xlsx` | Windows 显示扩展名导致重复后缀，改成正确文件名 |
| Excel 写入报错 `tuple` 不能赋值 | `iter_rows` 返回 Cell，需写 `.value` |
| 账号名差一点字就匹配不上 | 新增 `unmatched_account`，转人工核对（如「渠工」vs「泵工」） |
| 企业微信群机器人要建内部群 | 演示阶段改为：网页生成人工确认单 → 手动发给负责人；后续可用自建应用点对点推送 |
| 有异常时谁来拍板 | 明确：系统只发现问题，最终确认必须人工；已实现人工确认单流程 |

---

## 五、使用了哪些 AI 工具

| 工具 | 用途 |
|---|---|
| Cursor（Composer / Agent） | 辅助写代码、排查 bug、整理架构与交付文档 |
| （可选）DeepSeek / 阿里云 API | 规划中：文本格式不固定时做字段抽取兜底；**当前演示未依赖 API Key** |

说明：当前 MVP **不调用大模型 API** 也能完整演示；金额核对与异常判定全部是本地规则。

---

## 六、AI 生成了什么，我做了哪些检查 / 修改

### AI 辅助生成的内容

- 项目骨架：`extractor` / `validator` / `excel_writer` / `web_app`
- 网页演示页、负责人汇报模板
- 人工确认单流转（`human_review.py`）
- 交付说明文档草稿

### 我做的检查与修改（重点可讲）

1. **核对业务规则**：确认「声明总额 ≠ 明细合计」「打款日期早于申请日」等必须由代码判定，不能交给 AI 拍板  
2. **安全边界**：原表只读，输出必须是测试副本  
3. **真实表格验证**：用飞书下载的推广账号表实测，确认表头列齐全、昵称匹配逻辑正确  
4. **修 bug**：Excel 写入 `.value`、文件名双后缀、未匹配账号检测  
5. **产品决策**：暂不接企业微信群机器人；先把「异常 → 人工确认单」做扎实  
6. **负责人配置**：`config/responsible.json` 可改姓名，演示时指派给真实负责人  

---

## 七、当前能力 vs 后续规划

| 已完成 | 后续（可口头说明） |
|---|---|
| 文本解析 + 规则校验 | 大模型 fallback 解析不规则文本 |
| 测试表副本生成 | 确认/驳回状态留痕 |
| 异常 → 人工确认单 | 企业微信自建应用推送给指定负责人 |
| 网页一键演示 | OCR 截图输入 |

---

## 八、面试演示建议（2–3 分钟）

1. 打开网页，加载示例或粘贴「有异常」样例  
2. 指出：金额汇总是代码算的，不是 AI 估的  
3. 展示「已转人工确认」+ 人工确认单  
4. 打开下载的 Excel，指出「待人工确认」备注  
5. 打开 `validator.py` 或 `human_review.py`，讲 30 秒核心逻辑  
